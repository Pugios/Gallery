<ui:FluentWindow
    x:Class="Gallery2.Views.Windows.ImageWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Gallery2.Views.Windows"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"
    Title="{Binding ViewModel.Title}"
    Width="800"
    Height="700"
    ExtendsContentIntoTitleBar="True"
    WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="{Binding ViewModel.Title}"
            Grid.Row="0" />

        <Grid
            Grid.Row="1">
            <Viewbox
                Stretch="Uniform">
                <Grid
                    Width="{Binding ViewModel.NaturalWidth}"
                    Height="{Binding ViewModel.NaturalHeight}">
                    <Image
                        x:Name="DisplayImage"
                        Source="{Binding ViewModel.ImageSource}"
                        Stretch="Fill"
                        SizeChanged="OnImageSizeChanged" />
                    <Canvas
                        x:Name="FaceOverlay"
                        IsHitTestVisible="False" />
                </Grid>
            </Viewbox>
        </Grid>
    </Grid>
</ui:FluentWindow>
